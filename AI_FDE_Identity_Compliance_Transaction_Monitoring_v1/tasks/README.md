# Task Lifecycle
Use `backlog/`, `active/`, and `completed/` to make Cursor work on one governed increment at a time.

A task moves to `active/` only after Definition of Ready is met. It moves to `completed/` only after Definition of Done, traceability update and evidence capture.
